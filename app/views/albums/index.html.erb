<p id="notice"><%= notice %></p>

<h1>Albums</h1>

<!-- <% @albums.each do |al| %>
  <div class="jumbotron">
  <h2><%= al.title %> <small>(<%= al.year %>)</small></h2>
    <div class-"row">
    <div class="col-md-6">
  <%= image_tag al.albumart.url(:medium) %> </div>
    <div class="col-md-6">
    <% al.songs.each do |song| %>
      <p><%= song.title %> <%= song.length %></p>
    <% end %>   </div> </div> </div>
<% end %> -->


<% @albums.each do |al| %>
<div class="jumbotron">
<div class="row">
  <h2><%= al.title %> <small>(<%= al.year %>)</small></h2>
    <div class="col-md-6">
      <%= image_tag al.albumart.url(:medium) %>   </div>
        <% al.songs.each do |song| %>
          <div class="col-md-6">
            <h4><%= song.title %> <small><%= song.length %></small></h4> </div>
        <% end %>
</div> </div>
<% end %>

<%= link_to 'New Album', new_album_path %>
